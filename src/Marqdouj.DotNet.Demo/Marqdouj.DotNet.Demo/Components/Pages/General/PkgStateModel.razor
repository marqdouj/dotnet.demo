@page "/pkg-general-statemodel"
@rendermode InteractiveServer
@inject IDialogService DialogService

<PageTitle>@subject</PageTitle>

<FluentLayout>
    <PkgPageHeader Header="@subject"
                   HRefCode="@hRefCode"
                   Subject="@subject">
          <Content>
              <FluentLabel>
                Provides a base class for models that support state change notification, includinng suppression of notifications.
              </FluentLabel>
          </Content>     
    </PkgPageHeader>

    <FluentStack Orientation="Orientation.Vertical">

    </FluentStack>
</FluentLayout>

@code {
    private readonly string hRefCode = HRefCodeSource.General.CodeUrl("StateModel.cs");
    private readonly string subject = nameof(StateModel);
}
